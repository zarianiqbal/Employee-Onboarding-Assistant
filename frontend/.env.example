# Base URL for the backend API. Leave blank to use the Vite dev proxy (which
# forwards /api to http://localhost:8000). In production this is the App
# Service URL, or blank if the SPA is served from the same origin as the API.
VITE_API_BASE_URL=
